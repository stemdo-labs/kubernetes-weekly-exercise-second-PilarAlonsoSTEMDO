apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-palonso.fullname" . }}-test-connection"
  labels:
    {{- include "chart-palonso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-palonso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
